stages:
  - test

test-coverage:
  image: rocker/tidyverse:latest
  stage: test
  script:
    - R -e 'install.packages("covr")'
    - R -e 'cov <- covr::package_coverage(); cat(as.numeric(covr::percent_coverage(cov)), file="coverage.txt")'
  artifacts:
    paths:
      - coverage.txt