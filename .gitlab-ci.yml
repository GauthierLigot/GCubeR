stages:
  - test

test-coverage:
  image: rocker/tidyverse:latest
  stage: test
  script:
    - R -e 'install.packages("covr")'
    - R -e 'cov <- covr::package_coverage(); writeLines(covr::to_json(cov), "coverage.json")'
  artifacts:
    paths:
      - coverage.json